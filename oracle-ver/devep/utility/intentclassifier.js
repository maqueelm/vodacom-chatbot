module.exports = function () {
    /**
     * This validator will contains all functions that will decide which intent / entity is actually detected when multiple entities are returned
     * This validator will decide on the basis of context variables and entities to return the valid intent hit by user.
     */
};